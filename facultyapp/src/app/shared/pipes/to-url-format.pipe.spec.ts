import { ToUrlFormatPipe } from './to-url-format.pipe';

describe('ToUrlFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ToUrlFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
